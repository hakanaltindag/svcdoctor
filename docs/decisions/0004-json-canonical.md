# ADR 0004: JSON is canonical

## Decision

The canonical diagnostic report is a versioned JSON model. Terminal, Markdown, and future HTML outputs are derived from that model. Renderers contain no diagnosis logic.
